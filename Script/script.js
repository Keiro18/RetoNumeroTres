import datos from '../Data/datos.json'assert { type: "json" };
const comenzar = document.getElementById('comenzar');

console.log(datos);

comenzar.addEventListener('click', (e) => {
    let id = getElementById(datos).value;

});
